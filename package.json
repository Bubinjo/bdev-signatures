{
  "name": "bdev-signatures",
  "version": "0.1.0",
  "private": true,
  "description": "B.DEV Outlook add-in proof of concept for centrally managed signatures",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js && npm run validate",
    "validate": "node scripts/validate-manifest.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
